# Copie para .env.local (dev) ou configure no painel do Render (produção)

# URL do backend Django no Render — sem barra no final
# Em desenvolvimento deixe vazio (o proxy do Vite resolve)
VITE_API_BASE_URL=https://seu-backend.onrender.com
